import React from 'react';

import classes from './Button.css';

const button = () => (
    <div className={classes.ButtonSpan}>
        <button type="submit" className={classes.Button}>Find Out!</button>
    </div>
)

export default button;